{
  "name": "back-to-metal",
  "version": "1.1.0",
  "private": true,
  "description": "A step-by-step roadmap off AWS, Google Cloud and Azure onto Kubernetes you run yourself, typeset into a printable book, an EPUB and a static site.",
  "scripts": {
    "build": "python3 book/build.py && python3 book/render.py",
    "verify": "python3 book/verify.py"
  },
  "dependencies": {
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.0.0"
  }
}
